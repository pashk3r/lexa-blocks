# Тест-кейсы — Lexa-Blocks

**Тестировщик:** [egorka000](https://github.com/egorka000)  
**Основание:** SRS, Use Cases UC-1–UC-5, диаграммы классов и последовательности  

> **Важно:** «подпункты с буквой — связанные сценарии того же компонента».

---

## Условные обозначения

- **Приоритет:** P1 — критический, P2 — высокий, P3 — средний

---

## Блок 1. Игровое поле (Board)

### TC-01: Размер игрового поля

| Поле | Значение |
|------|----------|
| **ID** | TC-01 |
| **Приоритет** | P2 |
| **Связь** | SRS G1 |
| **Тест в скрипте** | `TestField::test_TC01_grid_size_is_8` |
| **Предусловие** | Импортирована константа `GRID_SIZE` |
| **Шаги** | 1. Проверить значение `GRID_SIZE`. |
| **Ожидаемый результат** | `GRID_SIZE == 8` |

---

### TC-02: Начальное состояние поля — все ячейки свободны

| Поле | Значение |
|------|----------|
| **ID** | TC-02 |
| **Приоритет** | P2 |
| **Тест в скрипте** | `TestField::test_TC02_initial_field_all_cells_free` |
| **Предусловие** | Создан объект `Board()` |
| **Шаги** | 1. Обойти все ячейки `board.grid`. 2. Проверить значение каждой. |
| **Ожидаемый результат** | Все 64 ячейки: `grid[r][c]` — falsy |

---

## Блок 2. Размещение фигур (UC-1)

### TC-03: Успешное размещение фигуры на свободную ячейку

| Поле | Значение |
|------|----------|
| **ID** | TC-03 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Happy Path |
| **Тест в скрипте** | `TestPlacement::test_TC03_can_place_single_cube_on_empty_cell` |
| **Предусловие** | Поле пустое |
| **Входные данные** | `make_block([(0,0)])`, позиция `gx=3, gy=3` |
| **Шаги** | 1. Вызвать `board.can_place(block, 3, 3)`. 2. Вызвать `board.place(block, 3, 3)`. 3. Проверить `board.grid[3][3]`. |
| **Ожидаемый результат** | `can_place == True`, `grid[3][3]` — занята |

---

### TC-04: Отклонение размещения на занятую ячейку

| Поле | Значение |
|------|----------|
| **ID** | TC-04 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A1 |
| **Тест в скрипте** | `TestPlacement::test_TC04_cannot_place_on_occupied_cell` |
| **Предусловие** | Ячейка `(gx=3, gy=3)` уже занята |
| **Входные данные** | `make_block([(0,0)])`, позиция `gx=3, gy=3` |
| **Шаги** | 1. Вызвать `board.can_place(block, 3, 3)`. |
| **Ожидаемый результат** | Возвращает `False` |

---

### TC-05: Отклонение размещения за границей поля

| Поле | Значение |
|------|----------|
| **ID** | TC-05 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A2 |
| **Тест в скрипте** | `TestPlacement::test_TC05_cannot_place_outside_border` |
| **Предусловие** | Поле 8×8 |
| **Входные данные** | Горизонтальная линия 3×1, позиция `gx=7, gy=0` |
| **Шаги** | 1. Вызвать `board.can_place(block, 7, 0)`. |
| **Ожидаемый результат** | Возвращает `False` |

---

### TC-06: Размещение квадрата 2×2

| Поле | Значение |
|------|----------|
| **ID** | TC-06 |
| **Приоритет** | P2 |
| **Связь** | SRS G2, фигура №1 |
| **Тест в скрипте** | `TestPlacement::test_TC06_place_2x2_square` |
| **Предусловие** | Поле пустое |
| **Входные данные** | `make_block([(0,0),(1,0),(0,1),(1,1)])`, позиция `gx=0, gy=0` |
| **Шаги** | 1. Вызвать `board.place(block, 0, 0)`. 2. Проверить ячейки. |
| **Ожидаемый результат** | `grid[0][0]`, `grid[0][1]`, `grid[1][0]`, `grid[1][1]` — все заняты |

---

### TC-06b: board.place() не принимает аргумент color=

| Поле | Значение |
|------|----------|
| **ID** | TC-06b |
| **Приоритет** | P2 |
| **Тест в скрипте** | `TestPlacement::test_TC06b_place_does_not_accept_color_kwarg` |
| **Предусловие** | Создан `Board()` |
| **Шаги** | 1. Вызвать `board.place([(0,0)], 0, 0, color=(255,0,0))`. |
| **Ожидаемый результат** | Выброс `TypeError: unexpected keyword argument` |

---

### TC-07: Панель содержит ровно 3 фигуры в начале игры

| Поле | Значение |
|------|----------|
| **ID** | TC-07 |
| **Приоритет** | P2 |
| **Связь** | SRS G2 |
| **Тест в скрипте** | `TestPlacement::test_TC07_initial_panel_has_3_figures` |
| **Предусловие** | `GameState()` только что создан |
| **Шаги** | 1. Проверить `len(state.blocks_in_hand)`. |
| **Ожидаемый результат** | `== 3` |

---

### TC-08: Новый набор генерируется после размещения всех трёх фигур

| Поле | Значение |
|------|----------|
| **ID** | TC-08 |
| **Приоритет** | P2 |
| **Связь** | SRS G2 |
| **Тест в скрипте** | `TestPlacement::test_TC08_new_set_spawned_after_placing_all_three` |
| **Предусловие** | `blocks_in_hand` очищен |
| **Шаги** | 1. Вызвать `state.spawn_blocks()`. 2. Проверить `len(blocks_in_hand)`. |
| **Ожидаемый результат** | `== 3` |

---

## Блок 3. Очистка линий и очки (UC-2)

### TC-09: Очистка заполненной строки

| Поле | Значение |
|------|----------|
| **ID** | TC-09 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC09_full_row_is_cleared` |
| **Предусловие** | Строка 0 полностью заполнена |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить все ячейки строки 0. |
| **Ожидаемый результат** | Все 8 ячеек строки 0 свободны |

---

### TC-10: Очистка заполненного столбца

| Поле | Значение |
|------|----------|
| **ID** | TC-10 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC10_full_col_is_cleared` |
| **Предусловие** | Столбец 0 полностью заполнен |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить все ячейки столбца 0. |
| **Ожидаемый результат** | Все 8 ячеек столбца 0 свободны |

---

### TC-11: 10 очков за одну линию

| Поле | Значение |
|------|----------|
| **ID** | TC-11 |
| **Приоритет** | P1 |
| **Связь** | SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC11_one_line_gives_10_points` |
| **Предусловие** | Одна строка заполнена |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `10` |

---

### TC-12: 20 очков за две одновременных линии

| Поле | Значение |
|------|----------|
| **ID** | TC-12 |
| **Приоритет** | P1 |
| **Связь** | SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC12_two_lines_give_20_points` |
| **Предусловие** | Строки 0 и 1 полностью заполнены |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `20` |

---

### TC-12b: 30 очков за три одновременных линии 

| Поле | Значение |
|------|----------|
| **ID** | TC-12b |
| **Приоритет** | P1 |
| **Связь** | SRS G3, **BUG-01** |
| **Тест в скрипте** | `TestLinesAndScore::test_TC12b_three_lines_score_bug` |
| **Предусловие** | Строки 0, 1, 2 полностью заполнены |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `30` |
| **Фактический результат** | Возвращает `60` |

---

### TC-12c: 40 очков за четыре одновременных линии 
| Поле | Значение |
|------|----------|
| **ID** | TC-12c |
| **Приоритет** | P1 |
| **Связь** | SRS G3, **BUG-01** |
| **Тест в скрипте** | `TestLinesAndScore::test_TC12c_four_lines_score_bug` |
| **Предусловие** | Строки 0, 1 и столбцы 0, 1 полностью заполнены |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `40` |
| **Фактический результат** | Возвращает `120` |

---

### TC-13: Повышение уровня при 100 очках

| Поле | Значение |
|------|----------|
| **ID** | TC-13 |
| **Приоритет** | P2 |
| **Связь** | SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC13_level_increases_at_100_points` |
| **Предусловие** | `state.score = 99`, `state.level = 1` |
| **Шаги** | 1. Вызвать `state._add_score(1)`. 2. Проверить `state.level`. |
| **Ожидаемый результат** | `state.level == 2` |

---

### TC-14: Прогресс: 50 очков = 50%

| Поле | Значение |
|------|----------|
| **ID** | TC-14 |
| **Приоритет** | P3 |
| **Связь** | SRS G3 |
| **Тест в скрипте** | `TestLinesAndScore::test_TC14_progress_50_points_equals_50_percent` |
| **Предусловие** | `state.score = 0` |
| **Шаги** | 1. Вызвать `state._add_score(50)`. 2. Вычислить `state.score % 100`. |
| **Ожидаемый результат** | `== 50` |

---

## Блок 4. Условие тупика (UC-3)

### TC-15: Переход game_over при тупике

| Поле | Значение |
|------|----------|
| **ID** | TC-15 |
| **Приоритет** | P1 |
| **Связь** | UC-3, SRS G4 |
| **Тест в скрипте** | `TestGameOver::test_TC15_game_over_when_no_moves` |
| **Предусловие** | Поле заполнено полностью |
| **Шаги** | 1. Вызвать `state._is_game_over()`. |
| **Ожидаемый результат** | Возвращает `True` |

---

### TC-16: При наличии хода game_over не срабатывает

| Поле | Значение |
|------|----------|
| **ID** | TC-16 |
| **Приоритет** | P2 |
| **Связь** | SRS G4 |
| **Тест в скрипте** | `TestGameOver::test_TC16_no_game_over_when_moves_available` |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Вызвать `state._is_game_over()`. |
| **Ожидаемый результат** | Возвращает `False` |

---

## Блок 5. Переходы состояний викторины (UC-4)

### TC-17: game_over=True → QuizPlugin переводит в STATE_CHOICE

| Поле | Значение |
|------|----------|
| **ID** | TC-17 |
| **Приоритет** | P1 |
| **Связь** | UC-4 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC17_on_game_over_sets_state_choice` |
| **Предусловие** | `state.game_over = True` |
| **Шаги** | 1. Вызвать `plugin.on_game_over(state, renderer)`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == STATE_CHOICE` |

---

### TC-18: Выбор типа вопроса → STATE_LOADING

| Поле | Значение |
|------|----------|
| **ID** | TC-18 |
| **Приоритет** | P1 |
| **Связь** | UC-4 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC18_fetch_sets_state_loading` |
| **Предусловие** | `state.go_state == STATE_CHOICE`, `api_client.fetch_question_async` замокан |
| **Шаги** | 1. Вызвать `plugin._fetch(state)`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == STATE_LOADING` |

---

### TC-19: Успешный ответ API → STATE_QUESTION с вопросом типа choice

| Поле | Значение |
|------|----------|
| **ID** | TC-19 |
| **Приоритет** | P1 |
| **Связь** | UC-4 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC19_on_success_sets_state_question` |
| **Предусловие** | API замокан, возвращает вопрос с 4 вариантами |
| **Шаги** | 1. Вызвать `plugin._on_success(state, "вопрос?", ["A)..","B)..","C)..","D).."], 1)`. 2. Проверить `state`. |
| **Ожидаемый результат** | `go_state == STATE_QUESTION`, `question_text` заполнен, `len(options) == 4`, `correct_index == 1` |

---

### TC-20: Неправильный ответ → STATE_RESULT_FAIL → полный сброс

| Поле | Значение |
|------|----------|
| **ID** | TC-21 |
| **Приоритет** | P1 |
| **Связь** | UC-4 Alt A1, SRS G8 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC21_wrong_answer_resets_game` |
| **Предусловие** | `go_state == STATE_QUESTION`, `correct_index == 1`, `state.score = 150` |
| **Шаги** | 1. Кликнуть на неверный вариант (индекс `0`). 2. Проверить `go_state`. 3. Кликнуть на экране результата. 4. Проверить state. |
| **Ожидаемый результат** | `go_state == STATE_RESULT_FAIL` → после клика: `score == 0`, `level == 1`, `game_over == False` |

---

### TC-21: Кнопка «Начать заново» → полный сброс

| Поле | Значение |
|------|----------|
| **ID** | TC-22 |
| **Приоритет** | P2 |
| **Связь** | UC-4 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC22_restart_button_resets_game` |
| **Предусловие** | `go_state == STATE_CHOICE`, `score = 150` |
| **Шаги** | 1. Кликнуть кнопку Restart (rects[0]) через `plugin._handle_choice_click`. 2. Проверить state. |
| **Ожидаемый результат** | `score == 0`, `level == 1`, `game_over == False` |

---

### TC-22: Ошибка API → фейковый вопрос → STATE_QUESTION (задокументированное поведение)

| Поле | Значение |
|------|----------|
| **ID** | TC-23 |
| **Приоритет** | P2 |
| **Связь** | UC-4 Exception E1 |
| **Тест в скрипте** | `TestQuizPlugin::test_TC23_api_error_shows_fake_question` |
| **Предусловие** | API выбрасывает исключение |
| **Шаги** | 1. Вызвать `plugin._on_error(state, "Таймаут соединения")`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `go_state == STATE_QUESTION` |
| **Примечание** | Задокументированное поведение — `make_error_question()` генерирует резервный вопрос. Дефектом не считается. |

---

## Блок 6. Спасательные кубы (UC-5)

### TC-23: spawn_rescue_blocks генерирует 3 куба 1×1

| Поле | Значение |
|------|----------|
| **ID** | TC-24 |
| **Приоритет** | P2 |
| **Связь** | UC-5, SRS G5 |
| **Тест в скрипте** | `TestRescueCubes::test_TC24_spawn_rescue_blocks_gives_3_single_cubes` |
| **Предусловие** | `GameState()` создан |
| **Шаги** | 1. Вызвать `state.spawn_rescue_blocks()`. 2. Проверить `blocks_in_hand`. |
| **Ожидаемый результат** | `len(blocks_in_hand) == 3`, каждый блок: `shape == [(0,0)]` |

---

### TC-24: Спасательный куб завершает строку — очистка

| Поле | Значение |
|------|----------|
| **ID** | TC-25 |
| **Приоритет** | P2 |
| **Связь** | UC-5 |
| **Тест в скрипте** | `TestRescueCubes::test_TC25_rescue_cube_clears_line_when_completing_row` |
| **Предусловие** | Строка 0 заполнена на 7/8 |
| **Шаги** | 1. Разместить `make_block([(0,0)])` в `gx=7, gy=0`. 2. Проверить строку 0. |
| **Ожидаемый результат** | Строка 0 полностью очищена |

---

### TC-24b: board.place() не перезаписывает занятые ячейки 

| Поле | Значение |
|------|----------|
| **ID** | TC-25b |
| **Приоритет** | P2 |
| **Связь** | **BUG-02** |
| **Тест в скрипте** | `TestRescueCubes::test_TC25b_place_overwrites_occupied_cell` |
| **Предусловие** | Ячейка `(gx=0, gy=0)` уже занята блоком A |
| **Шаги** | 1. Разместить блок A в `(0,0)`. 2. Разместить блок B в ту же позицию. 3. Проверить `grid[0][0]`. |
| **Ожидаемый результат** | `grid[0][0]` содержит цвет блока A — повторное размещение не перезаписывает |
| **Фактический результат** | `grid[0][0]` содержит цвет блока B — ячейка перезаписана |
| **Примечание** | BUG-04 частично закрыт: `can_place()` защищает через `GameState`, но прямой вызов `board.place()` всё ещё перезаписывает |

---

## Блок 7. Граничные значения

### TC-25: Размещение в угол (0,0)

| Поле | Значение |
|------|----------|
| **ID** | TC-26 |
| **Приоритет** | P3 |
| **Тест в скрипте** | `TestBoundary::test_TC26_place_in_corner_0_0` |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить `make_block([(0,0)])` в `gx=0, gy=0`. |
| **Ожидаемый результат** | Размещение успешно, `grid[0][0]` занята |

---

### TC-26: Размещение в угол (7,7)

| Поле | Значение |
|------|----------|
| **ID** | TC-27 |
| **Приоритет** | P3 |
| **Тест в скрипте** | `TestBoundary::test_TC27_place_in_corner_7_7` |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить `make_block([(0,0)])` в `gx=7, gy=7`. |
| **Ожидаемый результат** | Размещение успешно, `grid[7][7]` занята |

---

### TC-27: apply_question и clear_question корректно работают

| Поле | Значение |
|------|----------|
| **ID** | TC-28 |
| **Приоритет** | P2 |
| **Тест в скрипте** | `TestBoundary::test_TC28_apply_and_clear_question` |
| **Предусловие** | `GameState()` создан |
| **Шаги** | 1. Вызвать `state.apply_question("Q?", ["A","B","C","D"], 1)`. 2. Проверить поля. 3. Вызвать `state.clear_question()`. 4. Проверить поля. |
| **Ожидаемый результат** | После apply: поля заполнены. После clear: `question_text == ""`, `options == []` |
