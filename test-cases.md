# Тест-кейсы — Lexa-Blocks

**Версия:** 1.0  
**Дата:** 14-05-2026  
**Тестировщик:** [egorka000]  
**Основание:** SRS v1.0, Use Cases UC-1 — UC-5, код коммита `7298517`

> **Важно:** тест-кейсы написаны под реальную структуру кода из ветки `development`.
> Реальные модули: `board.py`, `game_state.py`, `quiz_plugin.py`, `api_client.py`, `constants.py`.
> Классов `GameManager`, `Field`, `ScoreSystem` из диаграммы в коде нет — вместо них `Board` и `GameState`.

---

## Условные обозначения

- **Приоритет:** P1 — критический, P2 — высокий, P3 — средний
- **Статус:** Не выполнен / Пройден / Упал / Заблокирован

---

## Блок 1. Игровое поле (board.py)

### TC-01: Размер игрового поля

| Поле | Значение |
|------|----------|
| **ID** | TC-01 |
| **Приоритет** | P2 |
| **Связь** | SRS G1 |
| **Предусловие** | Создан объект `Board()` |
| **Шаги** | 1. Получить объект `Board`. 2. Проверить константу `GRID_SIZE` из `constants.py`. |
| **Ожидаемый результат** | `GRID_SIZE == 8` |
| **Статус** | Не выполнен |

---

### TC-02: Начальное состояние поля — все ячейки свободны

| Поле | Значение |
|------|----------|
| **ID** | TC-02 |
| **Приоритет** | P2 |
| **Предусловие** | Создан объект `Board()` |
| **Шаги** | 1. Обойти все ячейки сетки `board.grid`. 2. Проверить значение каждой. |
| **Ожидаемый результат** | Все 64 ячейки: `grid[r][c] == 0` (или `False`) |
| **Статус** | Не выполнен |

---

## Блок 2. Размещение фигур (UC-1)

### TC-03: Успешное размещение фигуры на свободные ячейки

| Поле | Значение |
|------|----------|
| **ID** | TC-03 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Happy Path |
| **Предусловие** | Поле пустое |
| **Входные данные** | Одиночный куб `[(0,0)]`, позиция `(3,3)` |
| **Шаги** | 1. Вызвать `board.can_place(shape, gx=3, gy=3)`. 2. Вызвать `board.place(shape, gx=3, gy=3)`. 3. Проверить `board.grid[3][3]`. |
| **Ожидаемый результат** | `can_place == True`, `grid[3][3] != 0` (ячейка занята) |
| **Статус** | Не выполнен |

---

### TC-04: Отклонение размещения на занятую ячейку

| Поле | Значение |
|------|----------|
| **ID** | TC-04 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A1 |
| **Предусловие** | Ячейка `(3,3)` уже занята |
| **Входные данные** | Одиночный куб `[(0,0)]`, позиция `(3,3)` |
| **Шаги** | 1. Вызвать `board.can_place(shape, gx=3, gy=3)`. |
| **Ожидаемый результат** | Возвращает `False` |
| **Статус** | Не выполнен |

---

### TC-05: Отклонение размещения за границей поля

| Поле | Значение |
|------|----------|
| **ID** | TC-05 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A2 |
| **Предусловие** | Поле 8×8 |
| **Входные данные** | Горизонтальная линия `[(0,0),(1,0),(2,0)]`, позиция `(0,7)` (выйдет за правую границу) |
| **Шаги** | 1. Вызвать `board.can_place(shape, gx=0, gy=7)`. |
| **Ожидаемый результат** | Возвращает `False` |
| **Статус** | Не выполнен |

---

### TC-06: Размещение квадрата 2×2

| Поле | Значение |
|------|----------|
| **ID** | TC-06 |
| **Приоритет** | P2 |
| **Связь** | SRS G2 фигура №1 |
| **Предусловие** | Поле пустое |
| **Входные данные** | `[(0,0),(1,0),(0,1),(1,1)]`, позиция `(0,0)` |
| **Шаги** | 1. Разместить фигуру. 2. Проверить ячейки `(0,0)`, `(1,0)`, `(0,1)`, `(1,1)`. |
| **Ожидаемый результат** | Все 4 ячейки заняты |
| **Статус** | Не выполнен |

---

### TC-07: Панель содержит ровно 3 фигуры в начале игры

| Поле | Значение |
|------|----------|
| **ID** | TC-07 |
| **Приоритет** | P2 |
| **Связь** | SRS G2 |
| **Предусловие** | `GameState()` только что создан |
| **Шаги** | 1. Проверить `len(state.blocks_in_hand)`. |
| **Ожидаемый результат** | `== 3` |
| **Статус** | Не выполнен |

---

### TC-08: Новый набор генерируется после размещения всех трёх фигур

| Поле | Значение |
|------|----------|
| **ID** | TC-08 |
| **Приоритет** | P2 |
| **Связь** | SRS G2 |
| **Предусловие** | В панели 3 фигуры |
| **Шаги** | 1. Разместить все 3 фигуры. 2. Проверить `blocks_in_hand`. |
| **Ожидаемый результат** | Панель снова содержит 3 фигуры |
| **Статус** | Не выполнен |

---

## Блок 3. Очистка линий и очки (UC-2)

### TC-09: Очистка заполненной строки

| Поле | Значение |
|------|----------|
| **ID** | TC-09 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3 |
| **Предусловие** | Строка 0 полностью заполнена (все 8 ячеек) |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить строку 0. |
| **Ожидаемый результат** | Все ячейки строки 0 свободны |
| **Статус** | Не выполнен |

---

### TC-10: Очистка заполненного столбца

| Поле | Значение |
|------|----------|
| **ID** | TC-10 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3 |
| **Предусловие** | Столбец 0 полностью заполнен |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить столбец 0. |
| **Ожидаемый результат** | Все ячейки столбца 0 свободны |
| **Статус** | Не выполнен |

---

### TC-11: 10 очков за одну линию

| Поле | Значение |
|------|----------|
| **ID** | TC-11 |
| **Приоритет** | P1 |
| **Связь** | SRS G3 |
| **Предусловие** | Одна строка заполнена |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённое значение очков. |
| **Ожидаемый результат** | Возвращает `10` |
| **Статус** | Не выполнен |

---

### TC-12: РАСХОЖДЕНИЕ — формула очков за несколько линий

| Поле | Значение |
|------|----------|
| **ID** | TC-12 |
| **Приоритет** | P1 |
| **Связь** | SRS G3 |
| **Предусловие** | 2 строки заполнены одновременно |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат (SRS)** | `20` очков (2 линии × 10) |
| **Фактический результат (код)** | `40` очков (`2 * 10 * max(1, 2-1) = 40`) — **формула в коде не соответствует SRS** |
| **Статус** | **Баг P1** |

---

### TC-13: Повышение уровня при 100 очках

| Поле | Значение |
|------|----------|
| **ID** | TC-13 |
| **Приоритет** | P2 |
| **Связь** | SRS G3 |
| **Предусловие** | `state.score = 99` |
| **Шаги** | 1. Добавить 1 очко к `state.score`. 2. Проверить `state.level`. |
| **Ожидаемый результат** | `level == 2` (формула: `score // 100 + 1`) |
| **Статус** | Не выполнен |

---

### TC-14: Прогресс-бар: 50 очков = 50%

| Поле | Значение |
|------|----------|
| **ID** | TC-14 |
| **Приоритет** | P3 |
| **Связь** | SRS G3 |
| **Предусловие** | `state.score = 50` |
| **Шаги** | 1. Вычислить `state.score % 100`. |
| **Ожидаемый результат** | `== 50` |
| **Статус** | Не выполнен |

---

## Блок 4. Условие тупика (UC-3)

### TC-15: Переход в game_over при тупике

| Поле | Значение |
|------|----------|
| **ID** | TC-15 |
| **Приоритет** | P1 |
| **Связь** | UC-3, SRS G4 |
| **Предусловие** | Поле заполнено так, что ни одна из 3 фигур в `blocks_in_hand` не помещается |
| **Шаги** | 1. Вызвать `state.try_place_active_block()` на последнюю доступную клетку. 2. Проверить `state.game_over`. |
| **Ожидаемый результат** | `state.game_over == True` |
| **Статус** | Не выполнен |

---

### TC-16: При наличии хода — game_over не срабатывает

| Поле | Значение |
|------|----------|
| **ID** | TC-16 |
| **Приоритет** | P2 |
| **Связь** | SRS G4 |
| **Предусловие** | На поле есть свободные ячейки, хотя бы одна фигура может быть размещена |
| **Шаги** | 1. Разместить фигуру. 2. Проверить `state.game_over`. |
| **Ожидаемый результат** | `state.game_over == False` |
| **Статус** | Не выполнен |

---

## Блок 5. Переходы состояний викторины (UC-4)

### TC-17: При game_over плагин переводит в STATE_CHOICE

| Поле | Значение |
|------|----------|
| **ID** | TC-17 |
| **Приоритет** | P1 |
| **Связь** | UC-4, диаграмма состояний |
| **Предусловие** | `state.game_over = True`, плагин `QuizPlugin` подключён |
| **Шаги** | 1. Вызвать `plugin.on_game_over(state, renderer_mock)`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == "choice"` |
| **Статус** | Не выполнен |

---

### TC-18: Нажатие «Ответить на вопрос» переводит в STATE_LOADING

| Поле | Значение |
|------|----------|
| **ID** | TC-18 |
| **Приоритет** | P1 |
| **Связь** | UC-4 |
| **Предусловие** | `state.go_state == "choice"` |
| **Шаги** | 1. Вызвать `plugin._fetch(state)`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == "loading"` |
| **Статус** | Не выполнен |

---

### TC-19: После получения вопроса — переход в STATE_QUESTION

| Поле | Значение |
|------|----------|
| **ID** | TC-19 |
| **Приоритет** | P1 |
| **Связь** | UC-4, SRS G7 |
| **Предусловие** | API замокан, возвращает валидный вопрос |
| **Шаги** | 1. Вызвать `plugin._on_success(state, "2+2=?", ["A)3","B)4","C)5","D)6"], 1)`. 2. Проверить `state.go_state` и поля вопроса. |
| **Ожидаемый результат** | `go_state == "question"`, `state.question_text == "2+2=?"`, `len(state.options) == 4` |
| **Статус** | Не выполнен |

---

### TC-20: Правильный ответ → STATE_RESULT_OK → +3 куба → PLAYING

| Поле | Значение |
|------|----------|
| **ID** | TC-20 |
| **Приоритет** | P1 |
| **Связь** | UC-4 Happy Path, SRS G5, G9 |
| **Предусловие** | `go_state == "question"`, `correct_index == 1`, счёт и поле заданы |
| **Шаги** | 1. Симулировать клик на вариант с индексом `1` через `plugin._handle_question_click`. 2. Проверить `go_state`. 3. Симулировать клик на `STATE_RESULT_OK`. 4. Проверить `state.game_over`, `blocks_in_hand`, счёт. |
| **Ожидаемый результат** | `game_over == False`, `len(blocks_in_hand) == 3` (спасательные кубы 1×1), счёт не изменился |
| **Статус** | Не выполнен |

---

### TC-21: Неправильный ответ → STATE_RESULT_FAIL → полный сброс

| Поле | Значение |
|------|----------|
| **ID** | TC-21 |
| **Приоритет** | P1 |
| **Связь** | UC-4 Alt A1, SRS G8 |
| **Предусловие** | `go_state == "question"`, `correct_index == 1`, `state.score = 150` |
| **Шаги** | 1. Симулировать клик на неверный вариант (индекс `0`). 2. Проверить `go_state`. 3. Симулировать клик на `STATE_RESULT_FAIL`. 4. Проверить весь state. |
| **Ожидаемый результат** | `score == 0`, `level == 1`, поле пустое, `game_over == False` |
| **Статус** | Не выполнен |

---

### TC-22: Кнопка «Начать заново» в STATE_CHOICE → полный сброс

| Поле | Значение |
|------|----------|
| **ID** | TC-22 |
| **Приоритет** | P2 |
| **Связь** | UC-4 |
| **Предусловие** | `go_state == "choice"`, `score = 200` |
| **Шаги** | 1. Симулировать клик на кнопку Restart (`rects[0]`) через `plugin._handle_choice_click`. 2. Проверить state. |
| **Ожидаемый результат** | `score == 0`, `level == 1`, `game_over == False` |
| **Статус** | Не выполнен |

---

### TC-23: Ошибка API — показывается фейковый вопрос (не кнопка «Повторить»)

| Поле | Значение |
|------|----------|
| **ID** | TC-23 |
| **Приоритет** | P2 |
| **Связь** | UC-4 Exception E1, SRS G6 |
| **Предусловие** | API выбрасывает исключение |
| **Шаги** | 1. Вызвать `plugin._on_error(state, "Таймаут")`. 2. Проверить `go_state` и `question_text`. |
| **Ожидаемый результат (SRS)** | Показывается сообщение и кнопка **«Повторить»** |
| **Фактический результат (код)** | `go_state == "question"`, отображается фейковый вопрос с вариантами "A) Повторить / B) Сдаться" — **расхождение с SRS** |
| **Статус** | **Баг P2** |

---

## Блок 6. Спасательные кубы (UC-5)

### TC-24: spawn_rescue_blocks генерирует 3 куба 1×1

| Поле | Значение |
|------|----------|
| **ID** | TC-24 |
| **Приоритет** | P2 |
| **Связь** | UC-5 Happy Path, SRS G5 |
| **Предусловие** | Вызывается после правильного ответа |
| **Шаги** | 1. Вызвать `state.spawn_rescue_blocks()`. 2. Проверить `blocks_in_hand`. |
| **Ожидаемый результат** | `len(blocks_in_hand) == 3`, каждый блок имеет форму `[(0,0)]` |
| **Статус** | Не выполнен |

---

### TC-25: После размещения спасательного куба проверяются линии

| Поле | Значение |
|------|----------|
| **ID** | TC-25 |
| **Приоритет** | P2 |
| **Связь** | UC-5 |
| **Предусловие** | Строка 0 заполнена на 7/8, куб добавляет последнюю ячейку |
| **Шаги** | 1. Разместить куб в последнюю ячейку. 2. Проверить строку 0 и счёт. |
| **Ожидаемый результат** | Строка очищается, начисляются 10 очков |
| **Статус** | Не выполнен |

---

## Блок 7. Граничные значения

### TC-26: Размещение фигуры в угловую ячейку (0,0)

| Поле | Значение |
|------|----------|
| **ID** | TC-26 |
| **Приоритет** | P3 |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить одиночный куб в `(0,0)`. |
| **Ожидаемый результат** | Размещение успешно |
| **Статус** | Не выполнен |

---

### TC-27: Размещение фигуры в угловую ячейку (7,7)

| Поле | Значение |
|------|----------|
| **ID** | TC-27 |
| **Приоритет** | P3 |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить одиночный куб в `(7,7)`. |
| **Ожидаемый результат** | Размещение успешно |
| **Статус** | Не выполнен |

---

### TC-28: apply_question и clear_question корректно работают

| Поле | Значение |
|------|----------|
| **ID** | TC-28 |
| **Приоритет** | P2 |
| **Предусловие** | `GameState()` создан |
| **Шаги** | 1. Вызвать `state.apply_question("Q", ["A","B","C","D"], 2)`. 2. Проверить поля. 3. Вызвать `state.clear_question()`. 4. Проверить поля. |
| **Ожидаемый результат** | После apply: поля заполнены. После clear: `question_text == ""`, `options == []`, `selected_index == None` |
| **Статус** | Не выполнен |
