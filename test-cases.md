# Тест-кейсы — Lexa-Blocks
  
**Дата:** 22-05-2026  
**Тестировщик:** [egorka000](https://github.com/egorka000) 
**Основание:** [SRS](https://github.com/pashk3r/lexa_blocks/blob/analysis/docs/srs.md), [Use Cases](https://github.com/pashk3r/lexa_blocks/blob/analysis/docs/usecases.md) — сценарии UC-1 – UC-5, [Диаграмма классов](https://github.com/pashk3r/lexa_blocks/blob/design/generated/class-diagram.png), [Диаграмма состояний](https://github.com/pashk3r/lexa_blocks/blob/design/generated/state-machine-diagram.png),Диаграммы последовательности: [Размещение фигуры (UC-1)](https://github.com/pashk3r/lexa_blocks/blob/design/generated/sequence_place_figure.png), [Спасение через математический вопрос (UC-4)](https://github.com/pashk3r/lexa_blocks/blob/design/generated/sequence_loss_and_math.png), [Использование спасательного куба (UC-5)](https://github.com/pashk3r/lexa_blocks/blob/design/generated/sequence_use_save_cube.png),

> **Важно:** «подпункты с буквой — связанные сценарии того же компонента».

---

## Условные обозначения

- **Приоритет:** P1 — критический, P2 — высокий, P3 — средний
- **Статус:** Не выполнен / Пройден / Упал / Заблокирован

---

## Блок 1. Игровое поле (Board)

### TC-01: Размер игрового поля

| Поле | Значение |
|------|----------|
| **ID** | TC-01 |
| **Приоритет** | P2 |
| **Связь** | SRS G1 |
| **Предусловие** | Создан объект `Board()` |
| **Шаги** | 1. Получить объект поля. 2. Проверить ширину и высоту сетки. |
| **Ожидаемый результат** | Ширина = 8, высота = 8 |
| **Статус** | Не выполнен |

---

### TC-02: Начальное состояние поля — все ячейки свободны

| Поле | Значение |
|------|----------|
| **ID** | TC-02 |
| **Приоритет** | P2 |
| **Предусловие** | Создан объект `Board()` |
| **Шаги** | 1. Обойти все ячейки `board.grid`. 2. Проверить значение каждой. |
| **Ожидаемый результат** | Все 64 ячейки: `grid[r][c]` — falsy (свободна) |
| **Статус** | Не выполнен |

---

## Блок 2. Размещение фигур (UC-1, sequence_place_figure)

### TC-03: Успешное размещение фигуры на свободные ячейки

| Поле | Значение |
|------|----------|
| **ID** | TC-03 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Happy Path, sequence_place_figure: `can_place == True` → `place` |
| **Предусловие** | `GameState()` создан, поле пустое |
| **Входные данные** | Одиночный куб `Block(shape=[(0,0)])`, позиция `gx=3, gy=3` |
| **Шаги** | 1. Вызвать `board.can_place(block, 3, 3)`. 2. Вызвать `board.place(block, 3, 3)`. 3. Проверить `board.grid[3][3]`. |
| **Ожидаемый результат** | `can_place == True`, `grid[3][3]` — truthy (занята) |
| **Статус** | Не выполнен |

---

### TC-04: Отклонение размещения на занятую ячейку

| Поле | Значение |
|------|----------|
| **ID** | TC-04 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A1, sequence_place_figure: `can_place == False` → блок возвращается в слот |
| **Предусловие** | Ячейка `(3,3)` уже занята |
| **Входные данные** | Одиночный куб, позиция `gx=3, gy=3` |
| **Шаги** | 1. Вызвать `board.can_place(block, 3, 3)`. |
| **Ожидаемый результат** | Возвращает `False` |
| **Статус** | Не выполнен |

---

### TC-05: Отклонение размещения за границей поля

| Поле | Значение |
|------|----------|
| **ID** | TC-05 |
| **Приоритет** | P1 |
| **Связь** | UC-1 Alt A2 |
| **Предусловие** | Поле 8×8 |
| **Входные данные** | Горизонтальная линия 3×1, позиция `gx=7, gy=0` (третья ячейка выходит за правую границу) |
| **Шаги** | 1. Вызвать `board.can_place(block, 7, 0)`. |
| **Ожидаемый результат** | Возвращает `False` |
| **Статус** | Не выполнен |

---

### TC-06: Размещение квадрата 2×2

| Поле | Значение |
|------|----------|
| **ID** | TC-06 |
| **Приоритет** | P2 |
| **Связь** | SRS G2, фигура №1 |
| **Предусловие** | Поле пустое |
| **Входные данные** | `Block(shape=[(0,0),(1,0),(0,1),(1,1)])`, позиция `gx=0, gy=0` |
| **Шаги** | 1. Разместить фигуру. 2. Проверить ячейки (0,0), (1,0), (0,1), (1,1). |
| **Ожидаемый результат** | Все 4 ячейки заняты |
| **Статус** | Не выполнен |

---

### TC-06b: place() не принимает параметр color (API ограничение)

| Поле | Значение |
|------|----------|
| **ID** | TC-06b |
| **Приоритет** | P2 |
| **Связь** | Диаграмма классов `Board.place(block, gx, gy)` |
| **Предусловие** | Создан `Board()` |
| **Шаги** | 1. Вызвать `board.place([(0,0)], 0, 0, color=(255,0,0))`. |
| **Ожидаемый результат** | Метод генерирует `TypeError: unexpected keyword argument 'color'`. Цвет передаётся через `Block.color`. |
| **Статус** | Не выполнен |

---

### TC-07: Панель содержит ровно 3 фигуры в начале игры

| Поле | Значение |
|------|----------|
| **ID** | TC-07 |
| **Приоритет** | P2 |
| **Связь** | SRS G2, `GameState.blocks_in_hand` |
| **Предусловие** | `GameState()` только что создан |
| **Шаги** | 1. Проверить `len(state.blocks_in_hand)`. |
| **Ожидаемый результат** | `== 3` |
| **Статус** | Не выполнен |

---

### TC-08: Новый набор генерируется после размещения всех трёх фигур

| Поле | Значение |
|------|----------|
| **ID** | TC-08 |
| **Приоритет** | P2 |
| **Связь** | SRS G2, sequence_place_figure: `alt набор пуст → spawn_blocks()` |
| **Предусловие** | В `blocks_in_hand` 3 фигуры |
| **Шаги** | 1. Очистить `blocks_in_hand`. 2. Вызвать `state.spawn_blocks()`. 3. Проверить `len(blocks_in_hand)`. |
| **Ожидаемый результат** | `== 3` |
| **Статус** | Не выполнен |

---

## Блок 3. Очистка линий и очки (UC-2)

### TC-09: Очистка заполненной строки

| Поле | Значение |
|------|----------|
| **ID** | TC-09 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3, `Board._clear_lines` |
| **Предусловие** | Строка 0 полностью заполнена |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить строку 0. |
| **Ожидаемый результат** | Все ячейки строки 0 свободны |
| **Статус** | Не выполнен |

---

### TC-10: Очистка заполненного столбца

| Поле | Значение |
|------|----------|
| **ID** | TC-10 |
| **Приоритет** | P1 |
| **Связь** | UC-2, SRS G3, `Board._clear_lines` |
| **Предусловие** | Столбец 0 полностью заполнен |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить столбец 0. |
| **Ожидаемый результат** | Все ячейки столбца 0 свободны |
| **Статус** | Не выполнен |

---

### TC-11: 10 очков за одну линию

| Поле | Значение |
|------|----------|
| **ID** | TC-11 |
| **Приоритет** | P1 |
| **Связь** | SRS G3 |
| **Предусловие** | Одна строка заполнена |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `10` |
| **Статус** | Не выполнен |

---

### TC-12: 20 очков за две одновременных линии

| Поле | Значение |
|------|----------|
| **ID** | TC-12 |
| **Приоритет** | P1 |
| **Связь** | SRS G3: «2 линии → 20 очков» |
| **Предусловие** | Строки 0 и 1 полностью заполнены |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `20` |
| **Статус** | Не выполнен |

---

### TC-12b: 30 очков за три линии

| Поле | Значение |
|------|----------|
| **ID** | TC-12b |
| **Приоритет** | P1 |
| **Связь** | SRS G3: «3 линии → 30 очков» |
| **Предусловие** | Строки 0, 1, 2 полностью заполнены |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `30` (НЕ 60). Проверяет отсутствие квадратичной формулы. |
| **Статус** | Не выполнен |

---

### TC-12c: 40 очков за четыре линии 

| Поле | Значение |
|------|----------|
| **ID** | TC-12c |
| **Приоритет** | P1 |
| **Связь** | SRS G3: «4 линии → 40 очков» |
| **Предусловие** | Строки 0,1 и столбцы 0,1 полностью заполнены (4 линии) |
| **Шаги** | 1. Вызвать `board._clear_lines()`. 2. Проверить возвращённые очки. |
| **Ожидаемый результат** | Возвращает `40` (НЕ 120). Исправление: `lines * 10` вместо `lines * 10 * max(1, lines-1)`. |
| **Статус** | Не выполнен |

---

### TC-13: Повышение уровня при 100 очках

| Поле | Значение |
|------|----------|
| **ID** | TC-13 |
| **Приоритет** | P2 |
| **Связь** | SRS G3, `GameState._add_score` |
| **Предусловие** | `state.score = 99`, `state.level = 1` |
| **Шаги** | 1. Добавить 1 очко через `state._add_score(1)`. 2. Проверить `state.level`. |
| **Ожидаемый результат** | `state.level == 2` |
| **Статус** | Не выполнен |

---

### TC-14: Прогресс: 50 очков = 50%

| Поле | Значение |
|------|----------|
| **ID** | TC-14 |
| **Приоритет** | P3 |
| **Связь** | SRS G3 |
| **Предусловие** | `state.score = 50` |
| **Шаги** | 1. Вычислить `state.score % 100`. |
| **Ожидаемый результат** | `== 50` |
| **Статус** | Не выполнен |

---

## Блок 4. Условие тупика (UC-3)

### TC-15: Переход game_over при тупике

| Поле | Значение |
|------|----------|
| **ID** | TC-15 |
| **Приоритет** | P1 |
| **Связь** | UC-3, SRS G4, sequence_place_figure: `_is_game_over() → game_over = True` |
| **Предусловие** | Поле заполнено так, что ни одна из 3 фигур в `blocks_in_hand` не помещается |
| **Шаги** | 1. Вызвать `state._is_game_over()`. |
| **Ожидаемый результат** | Возвращает `True` |
| **Статус** | Не выполнен |

---

### TC-16: При наличии хода game_over не срабатывает

| Поле | Значение |
|------|----------|
| **ID** | TC-16 |
| **Приоритет** | P2 |
| **Связь** | SRS G4 |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Вызвать `state._is_game_over()`. |
| **Ожидаемый результат** | Возвращает `False` |
| **Статус** | Не выполнен |

---

## Блок 5. Переходы состояний викторины (UC-4, sequence_loss_and_math)

### TC-17: game_over=True → QuizPlugin переводит в STATE_CHOICE

| Поле | Значение |
|------|----------|
| **ID** | TC-17 |
| **Приоритет** | P1 |
| **Связь** | UC-4, sequence_loss_and_math: `on_game_over → go_state = STATE_CHOICE` |
| **Предусловие** | `state.game_over = True`, `QuizPlugin` подключён |
| **Шаги** | 1. Вызвать `plugin.on_game_over(state, renderer_mock)`. 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == STATE_CHOICE` |
| **Статус** | Не выполнен |

---

### TC-18: Клик «Ответить на вопрос» → STATE_LOADING

| Поле | Значение |
|------|----------|
| **ID** | TC-18 |
| **Приоритет** | P1 |
| **Связь** | UC-4, sequence_loss_and_math: `go_state = STATE_LOADING → fetch_question_async` |
| **Предусловие** | `state.go_state == STATE_CHOICE` |
| **Шаги** | 1. Вызвать `plugin._fetch(state)` (мок API). 2. Проверить `state.go_state`. |
| **Ожидаемый результат** | `state.go_state == STATE_LOADING` |
| **Статус** | Не выполнен |

---

### TC-19: Успешный ответ API → STATE_QUESTION с вопросом

| Поле | Значение |
|------|----------|
| **ID** | TC-19 |
| **Приоритет** | P1 |
| **Связь** | UC-4, sequence_loss_and_math: `apply_question → go_state = STATE_QUESTION` |
| **Предусловие** | API замокан, возвращает валидный вопрос |
| **Шаги** | 1. Вызвать `plugin._on_success(state, "2+2=?", ["A)3","B)4","C)5","D)6"], 1)`. 2. Проверить `state.go_state`, `question_text`, `options`. |
| **Ожидаемый результат** | `go_state == STATE_QUESTION`, `question_text == "2+2=?"`, `len(options) == 4` |
| **Статус** | Не выполнен |

---

### TC-20: Правильный ответ → STATE_RESULT_OK → spawn_rescue_blocks → STATE_GAME_OVER

| Поле | Значение |
|------|----------|
| **ID** | TC-20 |
| **Приоритет** | P1 |
| **Связь** | UC-4 Happy Path, sequence_loss_and_math: `STATE_RESULT_OK → spawn_rescue_blocks() → go_state = STATE_GAME_OVER` |
| **Предусловие** | `go_state == STATE_QUESTION`, `correct_index == 1` |
| **Шаги** | 1. Кликнуть на вариант с индексом `1`. 2. Проверить `go_state`. 3. Кликнуть на экране результата. 4. Проверить `game_over`, `blocks_in_hand`, `score`. |
| **Ожидаемый результат** | `go_state == STATE_RESULT_OK` → после клика `game_over == False`, `len(blocks_in_hand) == 3`, счёт не изменился |
| **Статус** | Не выполнен |

---

### TC-20b: Сохранение существующих фигур при спасательных кубах 

| Поле | Значение |
|------|----------|
| **ID** | TC-20b |
| **Приоритет** | P2 |
| **Связь** | SRS G9, UC-5 Happy Path |
| **Предусловие** | `game_over = True`, в `blocks_in_hand` уже есть 2 фигуры до тупика |
| **Шаги** | 1. Правильно ответить на вопрос викторины. 2. Проверить `blocks_in_hand`. |
| **Ожидаемый результат** | В руке должно быть 5 фигур (2 старые + 3 новых куба). `spawn_rescue_blocks()` должен дополнять руку, а не заменять полностью. |
| **Статус** | Не выполнен |

---

### TC-21: Неправильный ответ → STATE_RESULT_FAIL → полный сброс state.__init__()

| Поле | Значение |
|------|----------|
| **ID** | TC-21 |
| **Приоритет** | P1 |
| **Связь** | UC-4 Alt A1, sequence_loss_and_math: `STATE_RESULT_FAIL → state.__init__()` |
| **Предусловие** | `go_state == STATE_QUESTION`, `correct_index == 1`, `state.score = 150` |
| **Шаги** | 1. Кликнуть на неверный вариант (индекс `0`). 2. Проверить `go_state`. 3. Кликнуть на экране результата. 4. Проверить весь state. |
| **Ожидаемый результат** | `score == 0`, `level == 1`, поле пустое, `game_over == False` |
| **Статус** | Не выполнен |

---

### TC-22: Кнопка «Начать заново» → STATE_CHOICE → полный сброс

| Поле | Значение |
|------|----------|
| **ID** | TC-22 |
| **Приоритет** | P2 |
| **Связь** | UC-4, диаграмма состояний: `STATE_CHOICE → restart → STATE_GAME_OVER` |
| **Предусловие** | `go_state == STATE_CHOICE`, `score = 200` |
| **Шаги** | 1. Кликнуть кнопку Restart через `plugin._handle_choice_click`. 2. Проверить state. |
| **Ожидаемый результат** | `score == 0`, `level == 1`, `game_over == False` |
| **Статус** | Не выполнен |

---

### TC-23: Ошибка API → make_error_question → STATE_QUESTION (задокументированное поведение)

| Поле | Значение |
|------|----------|
| **ID** | TC-23 |
| **Приоритет** | P2 |
| **Связь** | UC-4 Exception E1, sequence_loss_and_math: `on_error → make_error_question()` |
| **Предусловие** | API выбрасывает исключение |
| **Шаги** | 1. Вызвать `plugin._on_error(state, "Таймаут")`. 2. Проверить `state.go_state` и `question_text`. |
| **Ожидаемый результат** | `go_state == STATE_QUESTION`, вопрос содержит опции «Повторить» и «Сдаться» |
| **Примечание** | Поведение задокументировано проектировщиком в `sequence_loss_and_math.puml` как намеренное. Дефектом не считается. |
| **Статус** | Не выполнен |

---

### TC-23b: clear_question() сбрасывает correct_index в None

| Поле | Значение |
|------|----------|
| **ID** | TC-23b |
| **Приоритет** | P3 |
| **Связь** | Диаграмма классов v2.0, `GameState.clear_question()` |
| **Предусловие** | Вопрос применён через `apply_question()` с `correct_index=2` |
| **Шаги** | 1. Вызвать `state.clear_question()`. 2. Проверить `state.correct_index`. |
| **Ожидаемый результат** | `correct_index == None`. (НЕ 0, чтобы случайный клик по варианту A не засчитался как правильный). |
| **Статус** | Не выполнен |

---

## Блок 6. Спасательные кубы (UC-5, sequence_use_save_cube)

### TC-24: spawn_rescue_blocks генерирует 3 куба 1×1

| Поле | Значение |
|------|----------|
| **ID** | TC-24 |
| **Приоритет** | P2 |
| **Связь** | UC-5 Happy Path, SRS G5, `GameState.spawn_rescue_blocks` |
| **Предусловие** | Вызывается после правильного ответа |
| **Шаги** | 1. Вызвать `state.spawn_rescue_blocks()`. 2. Проверить `blocks_in_hand`. |
| **Ожидаемый результат** | `len(blocks_in_hand) == 3`, каждый блок `shape == [(0,0)]` |
| **Статус** | Не выполнен |

---

### TC-25: Спасательный куб завершает строку — очистка + очки

| Поле | Значение |
|------|----------|
| **ID** | TC-25 |
| **Приоритет** | P2 |
| **Связь** | UC-5, sequence_use_save_cube |
| **Предусловие** | Строка 0 заполнена на 7/8, куб добавляет последнюю ячейку |
| **Шаги** | 1. Разместить куб в последнюю ячейку строки 0. 2. Вызвать `board._clear_lines()`. 3. Проверить строку и очки. |
| **Ожидаемый результат** | Строка очищается, возвращается `>= 10` очков |
| **Статус** | Не выполнен |

---

### TC-25b: place() не перезаписывает занятые ячейки (BUG-06)

| Поле | Значение |
|------|----------|
| **ID** | TC-25b |
| **Приоритет** | P2 |
| **Связь** | SRS G1 (целостность поля) |
| **Предусловие** | Ячейка `(0,0)` занята красным блоком |
| **Шаги** | 1. Вызвать `board.place(blue_block, 0, 0)`. 2. Проверить цвет ячейки `(0,0)`. |
| **Ожидаемый результат** | Ячейка сохраняет цвет красного блока. Метод должен генерировать исключение `ValueError` или возвращать `False` при попытке перезаписи. |
| **Статус** | Не выполнен |

---

## Блок 7. Граничные значения

### TC-26: Размещение в угол (0,0)

| Поле | Значение |
|------|----------|
| **ID** | TC-26 |
| **Приоритет** | P3 |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить одиночный куб в `gx=0, gy=0`. |
| **Ожидаемый результат** | Размещение успешно, `grid[0][0]` занята |
| **Статус** | Не выполнен |

---

### TC-27: Размещение в угол (7,7)

| Поле | Значение |
|------|----------|
| **ID** | TC-27 |
| **Приоритет** | P3 |
| **Предусловие** | Поле пустое |
| **Шаги** | 1. Разместить одиночный куб в `gx=7, gy=7`. |
| **Ожидаемый результат** | Размещение успешно, `grid[7][7]` занята |
| **Статус** | Не выполнен |

---

### TC-28: apply_question и clear_question корректно работают

| Поле | Значение |
|------|----------|
| **ID** | TC-28 |
| **Приоритет** | P2 |
| **Связь** | `GameState.apply_question`, `GameState.clear_question` из диаграммы классов v2.0 |
| **Предусловие** | `GameState()` создан |
| **Шаги** | 1. Вызвать `state.apply_question("Q?", ["A","B","C","D"], 2)`. 2. Проверить поля. 3. Вызвать `state.clear_question()`. 4. Проверить поля. |
| **Ожидаемый результат** | После apply: поля заполнены. После clear: `question_text == ""`, `options == []`, `selected_index == None` |
| **Статус** | Не выполнен |
